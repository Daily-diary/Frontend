const Login = () => {
  return (
    <div className="page page--no-nav" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <h1 style={{ color: 'var(--color-text-strong)', fontSize: 'var(--font-size-xl)' }}>로그인</h1>
    </div>
  );
};

export default Login;
